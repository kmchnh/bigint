sum
/*
max함수를 사용해 더 큰 자릿수를 구한다.

큰 자릿수에서 자기 자신의 자릿수를 빼고 zero padding을 하고 난 후 자기 자신을 다시 더해준다.

result에 첫자리에 0~9 숫자를 넣기 위해 digit%10을 수행한다.
digit%10을 통해 구한 값을 숫자로 변환하기 위해 ASCII code 에서 0을 더해준다.

첫번째숫자와 두번째숫자를 자릿수에 맞게 차례대로 더하고 올림값인 carry를 더해준다.

또한 숫자로 변환하기 위해 ASCII code 0을 더해준다.

첫 자릿수는 올림 값이 필요없기 때문에 carry = digitsum /10을 나중에 넣는다.

덧셈이 끝난 후 첫 자릿수에 올림값이 있으면 첫 자릿수 앞에 올림값을 추가한다.
*/

sub
/*
max함수를 사용해 더 큰 자릿수를 구한다.

두번째숫자가 첫번째 숫자보다 큰 경우 스왑한다.

각 자릿수의 뺄셈이 0보다 작을 때, 하나 위 자릿수에 1을 더해서 음수가 나오는 것을 방지한다.

결과값을 확인 해 숫자 앞에 불필요한 0을 제거한다.

만약 결과값이 음수일 경우 문자열 맨 앞에 -를 추가한다.
*/